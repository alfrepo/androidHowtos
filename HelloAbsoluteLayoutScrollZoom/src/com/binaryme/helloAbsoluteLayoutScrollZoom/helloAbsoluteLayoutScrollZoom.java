package com.binaryme.helloAbsoluteLayoutScrollZoom;

import android.app.Activity;
import android.graphics.Color;
import android.os.Bundle;
import android.view.ViewGroup;
import android.view.ViewGroup.LayoutParams;
import android.widget.LinearLayout;

public class helloAbsoluteLayoutScrollZoom extends Activity {
    /** Called when the activity is first created. */
    @Override
    public void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.main);
        
//        LinearLayout container = new LinearLayout(this);
//        container.setLayoutParams(new LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, ViewGroup.LayoutParams.MATCH_PARENT));
//        container.setOrientation(LinearLayout.VERTICAL);
//        container.setBackgroundColor(Color.RED);
//        
//        AbsoluteLayoutExtended layout = new AbsoluteLayoutExtended(this);
//        layout.setBackgroundColor(Color.YELLOW);
//        layout.setLayoutParams(new LinearLayout.LayoutParams(500, 500));
//        container.addView(layout);
//        
//        
//        setContentView(container);
        
    }
}